{
  "name": "server-notes",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/server/notes/src",
  "projectType": "library",
  "tags": ["server:notes"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/server/notes/jest.config.ts"
      }
    }
  }
}
